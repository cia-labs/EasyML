/*  
    appFlow.json checks for package.json in the root(i.e, frontend) and then build on top it
*/
{
    "apps": [
        {
            "appId": "9fc518cc", 
            "root": "./frontend"
            
        }
    ]
}
